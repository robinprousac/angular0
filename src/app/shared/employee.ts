export class Employee {
   id: string;
   name: string;
   email: string;
   phone: string;
}

/*
var  productos = array ();
cont = 0;

for each( pos as producto){
  cont++;
  productos[cont++] = {'id':pos.id, 'name':pos.name, 'precio':pos.precio};


}

res.json(productos);

*/
